name: Run Pipeline Task Tests
on:
  pull_request:
    paths:
      - "pipeline/tasks/**"
  push:
    branches:
      - main
    paths:
      - "pipeline/tasks/**"
jobs:
  retrieve:
    name: Retrieve Pipeline Tasks and their tests
    runs-on: ubuntu-latest
    outputs:
      tasks: ${{ steps.retrieve.outputs.tasks }}
    steps:
      - uses: actions/checkout@v6
      - name: Retrieve Pipeline Tasks and their tests
        id: retrieve
        run: |
          tasks=$(find pipeline/tasks -mindepth 1 -maxdepth 1 -type d \
            -exec test -d {}/tests \; -print | jq -R -s -c 'split("\n") | map(select(. != ""))')
          echo "tasks=$tasks" >> $GITHUB_OUTPUT
  test:
    name: Run Tests of ${{ matrix.task }}
    needs: retrieve
    if: ${{ needs.retrieve.outputs.tasks != '[]' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: ${{ fromJson(needs.retrieve.outputs.tasks) }}
    steps:
      - uses: actions/checkout@v6
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: |
            ${{ matrix.task }}/requirements.txt
            ${{ matrix.task }}/requirements-dev.txt
      - name: Install dependencies
        working-directory: ${{ matrix.task }}
        run: |
          python -m pip install --upgrade pip
          [ -f requirements.txt ] && pip install -r requirements.txt || true
          [ -f requirements-dev.txt ] && pip install -r requirements-dev.txt || true
          pip install pytest pytest-cov
      - name: Run tests
        working-directory: ${{ matrix.task }}
        env:
          PYTHONPATH: src
        run: |
          pytest tests/ --tb=short --cov=src --cov-report=term-missing --cov-report=xml:coverage.xml -v
      - name: Upload coverage report
        uses: actions/upload-artifact@v6
        with:
          name: coverage-${{ strategy.job-index }}
          path: ${{ matrix.task }}/coverage.xml
          retention-days: 7
